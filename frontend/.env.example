# Public API URL for the backend
# This is exposed to the browser, so it must be accessible from the client
NEXT_PUBLIC_API_URL="http://localhost:3008/api"

# Public API key for authentication
NEXT_PUBLIC_API_KEY="admin_api_key"

# Enable file watching in Docker for hot reload
# Set to true when running in Docker containers
WATCHPACK_POLLING=true
